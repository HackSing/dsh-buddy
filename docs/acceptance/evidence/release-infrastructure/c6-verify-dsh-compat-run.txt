== c6 verify-dsh-compat.js 本机对 0.1.0-rc.6 四项全绿 ==

$ TMPDIR=<scratchpad>/tmp node scripts/verify-dsh-compat.js 0.1.0-rc.6 --report <md> --summary <txt>
exit=0  summary=4/4 ✅

--- 报告全文 ---
# dsh 0.1.0-rc.6 兼容性验证

- 候选版本: `0.1.0-rc.6`
- 当前钉住: `0.1.0-rc.6`
- 结论: **4/4** 项通过
- 环境: darwin-arm64 / node v24.18.1
- 时间: 2026-08-16T08:26:21.142Z

| 项 | 结果 | 说明 |
| --- | --- | --- |
| ① npm install 与 bin 入口解析 | ✅ | 入口 node_modules/@deepseek-ai/dsh/lib/bin.js |
| ② 随包 preset 单元测试 | ✅ | plugins/dsh-anchored-standard 的 node --test 全绿 |
| ③ 随包 preset 装载后 web 启动 | ✅ | 装入 3 个 preset(preset, zero-anchored-standard, whoami-standard),web 返回 HTTP 200 |
| ④ manifest 六插件安装后 web 启动 | ✅ | 6 个插件安装到位,web 返回 HTTP 200 |

> 本工作流只报告,不会自动修改 package.json 里钉住的 dsh 版本。

--- 失败路径反证:传入不存在的版本,逐项独立 try 与非零退出 ---
$ node scripts/verify-dsh-compat.js 9.9.9-does-not-exist --report <md>
exit=1

# dsh 9.9.9-does-not-exist 兼容性验证

- 候选版本: `9.9.9-does-not-exist`
- 当前钉住: `0.1.0-rc.6`
- 结论: **1/4** 项通过
- 环境: darwin-arm64 / node v24.18.1
- 时间: 2026-08-16T08:26:49.194Z

| 项 | 结果 | 说明 |
| --- | --- | --- |
| ① npm install 与 bin 入口解析 | ❌ | npm 退出码 1 |
| ② 随包 preset 单元测试 | ✅ | plugins/dsh-anchored-standard 的 node --test 全绿 |
| ③ 随包 preset 装载后 web 启动 | ❌ | 前置步骤未解析出 dsh 入口,本项无法执行 |
| ④ manifest 六插件安装后 web 启动 | ❌ | 前置步骤未解析出 dsh 入口,本项无法执行 |

## 失败详情

### ① npm install 与 bin 入口解析

原因: npm 退出码 1

```
$ npm install --no-audit --no-fund @deepseek-ai/dsh@9.9.9-does-not-exist
npm error code ETARGET
npm error notarget No matching version found for @deepseek-ai/dsh@9.9.9-does-not-exist.

判读:① 失败后 ② 仍照常执行并通过,③④ 被标记为「前置步骤未解析出 dsh 入口」,
      汇总 1/4 且退出码 1 —— 符合「一项失败继续跑后面的,最终汇总」的合同。

--- 收尾核对 ---
$ ps -eo pid,command | grep -E "bin\.js web|dsh.*web --host"  → (无)
$ ls <TMPDIR>/dsh-compat-*                                      → (已清理)
临时工作区与两个临时 DSH_HOME 全在 TMPDIR 下,~/.dsh 未被触碰。
