# dsh-buddy-about bundle patch: inserts the plugin row into the web plugin
# roster. Client-only bundle (no host half): the modules node half scans
# dsh.client packages into the web plugin roster and serves this package's
# lib/client.js to the browser.
- insert:
    - id: dsh-buddy-about
      name: 'dsh-buddy-about'
